# Python zavislosti pro NMAP Scanner (PySide6 GUI)
# Instalace:  pip install -r requirements.txt
#
# Pozn.: PySide6 je meta-balicek, ktery stahne i PySide6-Addons
# (obsahuje QtWebEngine pouzivany pro screenshoty webovych sluzeb).

PySide6>=6.6
python-nmap>=0.7.1
requests>=2.31
python-docx>=1.1
selenium>=4.18
urllib3>=2.0
sslyze>=5.0
zaproxy>=0.3        # python klient OWASP ZAP daemon API (samotny ZAP se instaluje zvlast)

# --- Systemove nastroje (NEjsou pip balicky, instalovat zvlast) ---
#   nmap            scan portu/sluzeb          (brew install nmap)
#   ffuf            fuzzing adresaru           (brew install ffuf)
#   testssl.sh      TLS audit internich IP     (brew install testssl)
#   sslscan         TLS audit (engine)         (brew install sslscan)
#   openssl         detaily certifikatu        (soucast systemu)
#   chromedriver    selenium screenshoty       (brew install chromedriver)
#   OWASP ZAP       aktivni web sken (daemon)  (brew install --cask zap)
